```
usage: macs-logger.py [-h] [-d DEVICE] [-l LOGFMT] [-lf LOGFILE] [-q QUIET]

options:
  -h, --help            show this help message and exit
  -d DEVICE, --device DEVICE
                        TTY device
  -l LOGFMT, --logfmt LOGFMT
                        Log format: csv, text
  -lf LOGFILE, --logfile LOGFILE
                        Log file location
  -q QUIET, --quiet QUIET
                        Quietly log to a file
```
